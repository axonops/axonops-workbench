/**
 * This module is calling the `logging` module in `custom_node_modules/main`
 * By calling the module in this way we keep the consistency of calling modules of each thread
 */
module.exports = {
  ...require('../main/logging')
}
